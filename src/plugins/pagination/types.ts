import type {PaginatedResponseData, Response} from '../../request/types'
import type {
  APIClient
} from '../../client/types'

export type { APIClient, PaginatedResponseData, Response }
export type Direction = 'next' | 'previous'
